# =============================================================================
# SVA Backend — Runtime Dependencies
# =============================================================================
# Pinned to minor versions. Patch updates are safe.
# Install in venv: pip install -r requirements.txt

# --- Web framework ---
fastapi>=0.141.1,<1.0.0
uvicorn[standard]>=0.52.4,<1.0.0

# --- Data validation ---
pydantic>=2.13.4,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# --- Database (PostgreSQL-compatible ORM) ---
sqlalchemy[asyncio]>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0
aiosqlite>=0.20.0,<1.0.0     # Dev: SQLite async backend

# --- Parsing ---
tree-sitter>=0.24.0,<1.0.0
tree-sitter-python>=0.24.0,<1.0.0
tree-sitter-javascript>=0.23.0,<1.0.0
tree-sitter-typescript>=0.23.0,<1.0.0

# --- File / encoding utilities ---
chardet>=5.2.0,<6.0.0

# --- Utilities ---
python-dotenv>=1.2.3,<2.0.0
PyYAML>=6.0.3,<7.0.0
python-multipart>=0.0.32,<1.0.0
argon2-cffi>=23.1.0
email-validator>=2.0.0

# --- Provider Integrations ---
cryptography>=43.0.0
httpx>=0.27.0
